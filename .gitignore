node_modules/
dist/
build/
.DS_Store
*.log
.vite/
coverage/
.env.local
.idea/
.vscode/

# Claude worktrees + transient screenshots + per-machine local settings
.claude/worktrees/
.claude/*.png
.claude/settings.local.json
.claude/scheduled_tasks.lock
.phase*.png
.diag*.png

# Superpowers brainstorm scratch
.superpowers/

# Internal scaffolding — not part of the public repo
docs/superpowers/
docs/flam3-local-build.md
.claude/agents/wgsl-parity-reviewer.md
.claude/agents/flame-fixture-investigator.md

# Parity investigation scratch (uncommitted PNGs, captured JSON buffers)
.parity-investigation/

# Remember / session state
.remember/tmp/
.remember/logs/

# Phase 2 parity test artifacts (regenerated each run)
fixtures/flam3-goldens/*/diff.png
fixtures/flam3-goldens/*/pyr3-render.png
fixtures/flam3-goldens/*/pyr3-fe-render.png
fixtures/flam3-goldens/*/pyr3-fe-diff.png
# PYR3-026 FE↔BE parity rig artifacts (regenerated each run)
fixtures/flam3-goldens/*/pyr3-fe-be-be.png
fixtures/flam3-goldens/*/pyr3-fe-be-fe.png
fixtures/flam3-goldens/*/fe-be-diff.png
.remember/verify/

# PYR3-023 probe artifacts (regenerated; large derived PNGs + verbatim ESF copies)
fixtures/pyr3-4k-renders/
fixtures/showcase-probe-sources/

# v1.0 showcase renders (55 × ~2MB 4K PNGs; regenerated by
# scripts/render-showcase-v1.0.mjs). Manifest JSON kept for the
# scripts/build-showcase-v1.0-gallery.mjs lookup table.
fixtures/showcase-v1.0/*.pyr3-4k.png

# Generated public /showcase gallery (heavy JPEGs + .flames + index.html;
# regenerated by scripts/build-showcase.mjs, deployed to gh-pages via the
# Vite build artifact — never committed to main).
public/showcase/

# Corpus chunks for LOCAL dev-server testing of the /v1/gen/{gen}/id/{id}
# route. Production bakes these from the ESF Release at deploy time
# (deploy.yml) — they are deploy artifacts and must NEVER be committed.
# Copy in locally with: cp -R dist/chunks public/chunks
public/chunks/
