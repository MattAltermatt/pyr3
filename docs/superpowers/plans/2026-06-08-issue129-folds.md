# Fold-Family Variations Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Implement 4 novel fold variations (box_fold, sphere_fold, mandelbox_step, kifs_fold) for Issue #129.

**Architecture:** Stateless WGSL coordinate warps. No DC coloring in this slice.

**Tech Stack:** TypeScript, WGSL, Vitest.

---

### Task 1: Add to Variation Registry

**Files:**
- Modify: `src/variations.ts`
- Modify: `src/serialize.ts`

- [ ] **Step 1: Update `V` enum**
Add `box_fold: 233`, `sphere_fold: 234`, `mandelbox_step: 235`, `kifs_fold: 236`.

- [ ] **Step 2: Update parameters mapping**
Add parameter names to `VARIATION_PARAMS` and defaults to `VARIATION_DEFAULTS` in `src/serialize.ts`:
- `box_fold`: `['limit']`, default `[1.0]`
- `sphere_fold`: `['rmin', 'rmax']`, default `[0.5, 1.0]`
- `mandelbox_step`: `['scale', 'rmin', 'rmax', 'cx', 'cy']`, default `[2.0, 0.5, 1.0, 0.0, 0.0]`
- `kifs_fold`: `['n', 'offset']`, default `[3.0, 0.0]`

### Task 2: Implement WGSL Warps

**Files:**
- Modify: `src/shaders/chaos.wgsl`

- [ ] **Step 1: Add mathematical functions**
Add `var_box_fold`, `var_sphere_fold`, `var_mandelbox_step`, `var_kifs_fold`.

- [ ] **Step 2: Hook up in switch statement**
Add `case 233u` to `case 236u` in `evaluate_variation`.

### Task 3: Catalog and UI Integration

**Files:**
- Modify: `src/variation-catalog-data.ts`
- Modify: `src/edit-variation-picker.ts`

- [ ] **Step 1: Update Catalog**
Add JS warpFn equivalents, formulas, blurbs, and params to `CATALOG_DATA`. Update `sourceForIdx` to return 'novel'.

- [ ] **Step 2: Update Picker UI**
Add a "Folds & IFS" category to `CATEGORY_MAP` and include these 4 variations.

### Task 4: GPU Tests

**Files:**
- Create: `src/issue129-folds.gpu.test.ts`

- [ ] **Step 1: Write GPU Tests**
Test output finiteness and basic reflection behavior.

- [ ] **Step 2: Run tests**
Run `npm run test -- src/issue129-folds.gpu.test.ts` to verify.
