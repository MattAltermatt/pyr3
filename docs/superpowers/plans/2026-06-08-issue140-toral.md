# Toral Chaos Maps Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Implement 4 novel toral chaos map variations (arnold_cat, bakers_map, tent_map, logistic_map) for Issue #140.

**Architecture:** Stateless WGSL coordinate warps bounded by `fract(p + 0.5) - 0.5`.

**Tech Stack:** TypeScript, WGSL, Vitest.

---

### Task 1: Add to Variation Registry

**Files:**
- Modify: `src/variations.ts`
- Modify: `src/serialize.ts`

- [ ] **Step 1: Update `V` enum**
Add `arnold_cat: 237`, `bakers_map: 238`, `tent_map: 239`, `logistic_map: 240`.

- [ ] **Step 2: Update parameters mapping**
Add parameter names to `VARIATION_PARAMS` and defaults to `VARIATION_DEFAULTS` in `src/serialize.ts`:
- `logistic_map`: `['r']`, default `[3.9]`
- Others have no parameters.

### Task 2: Implement WGSL Warps

**Files:**
- Modify: `src/shaders/chaos.wgsl`

- [ ] **Step 1: Add mathematical functions**
Add `var_arnold_cat`, `var_bakers_map`, `var_tent_map`, `var_logistic_map`.

- [ ] **Step 2: Hook up in switch statement**
Add `case 237u` to `case 240u` in `evaluate_variation`.

### Task 3: Catalog Integration

**Files:**
- Modify: `src/variation-catalog-data.ts`

- [ ] **Step 1: Update Catalog**
Add JS warpFn equivalents, formulas, blurbs, and params to `CATALOG_DATA`. Update `sourceForIdx` to return 'novel'.

### Task 4: GPU Tests

**Files:**
- Create: `src/issue140-toral.gpu.test.ts`

- [ ] **Step 1: Write GPU Tests**
Test output finiteness and bounding behavior.

- [ ] **Step 2: Run tests**
Run `npm run test -- src/issue140-toral.gpu.test.ts` to verify.
